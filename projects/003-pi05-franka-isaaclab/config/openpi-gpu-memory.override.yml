services:
  openpi_server:
    environment:
      - SERVER_ARGS
      # 16 GB GPU 需要同时运行 π0.5 与 Isaac Sim。
      - XLA_PYTHON_CLIENT_MEM_FRACTION=0.50
      # 避免首次推理为 CUDA graph/command buffer 额外申请一块显存。
      - XLA_FLAGS=--xla_gpu_enable_command_buffer=
